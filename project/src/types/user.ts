export type User = {
  avatarUrl: string;
  id: number;
  isPro: boolean;
  name: string;
};

export type AuthUser = {
  avatarUrl: string;
  id: number;
  isPro: boolean;
  name: string;
  email: string;
};
